<div class="max-w-3xl mx-auto space-y-8">
  <!-- Controls Section -->
  <div>
    <p class="text-xs font-semibold uppercase tracking-[0.2em] text-cyan-300 mb-2">Service</p>
    <h2 class="text-3xl font-semibold text-white mb-2">Settings</h2>
    <p class="text-sm text-gray-400 mb-6">
      Control the uploader and map local NAS folders to Proton Drive destinations.
    </p>

    <!-- First row: Pause/Resume and Shut Down -->
    <div class="grid grid-cols-1 lg:grid-cols-2 gap-6 [&:has(#stop-section:empty)]:lg:grid-cols-1">
      <!-- Pause/Resume Sync -->
      {{CONTROLS_PAUSE_CONTENT}}

      <!-- Stop Service -->
      <div id="stop-section" hx-swap="innerHTML" sse-swap="stop-section">
        {{STOP_SECTION_CONTENT}}
      </div>
    </div>

    <!-- Second row: Start on Login -->
    <div class="grid grid-cols-1 lg:grid-cols-2 gap-6 mt-6">
      <!-- Start on Login Toggle -->
      {{START_ON_LOGIN_SECTION}}
    </div>
  </div>

  <!-- Secure Proton authentication (explicit server-side opt-in) -->
  <div id="web-auth-section" class="hidden">
    <h2 class="text-2xl font-semibold text-white mb-2">Proton account</h2>
    <p class="text-sm text-gray-400 mb-6">
      Sign in without opening a container shell. Credentials are sent only to Proton and reusable
      session tokens are stored in the encrypted config volume.
    </p>

    <div class="bg-gray-800 rounded-xl border border-gray-700 p-6">
      <div id="web-auth-unavailable" class="hidden text-sm text-amber-300"></div>

      <form id="web-auth-login-form" class="space-y-4" autocomplete="on">
        <div>
          <label for="web-auth-access-token" class="block text-xs text-gray-400 mb-1.5"
            >Dashboard login token</label
          >
          <input
            id="web-auth-access-token"
            type="password"
            required
            minlength="32"
            maxlength="1024"
            autocomplete="off"
            class="w-full px-3 py-2 bg-gray-900 border border-gray-600 rounded-lg text-white focus:outline-none focus:border-proton"
          />
        </div>
        <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
          <div>
            <label for="web-auth-username" class="block text-xs text-gray-400 mb-1.5"
              >Proton email or username</label
            >
            <input
              id="web-auth-username"
              type="text"
              required
              maxlength="320"
              autocomplete="username"
              class="w-full px-3 py-2 bg-gray-900 border border-gray-600 rounded-lg text-white focus:outline-none focus:border-proton"
            />
          </div>
          <div>
            <label for="web-auth-password" class="block text-xs text-gray-400 mb-1.5"
              >Proton password</label
            >
            <input
              id="web-auth-password"
              type="password"
              required
              maxlength="1024"
              autocomplete="current-password"
              class="w-full px-3 py-2 bg-gray-900 border border-gray-600 rounded-lg text-white focus:outline-none focus:border-proton"
            />
          </div>
        </div>
        <button
          id="web-auth-login-button"
          type="submit"
          class="px-4 py-2 bg-proton hover:bg-proton-dark text-white text-sm font-medium rounded-lg transition-colors disabled:opacity-50"
        >
          Sign in securely
        </button>
      </form>

      <form id="web-auth-challenge-form" class="hidden space-y-4" autocomplete="off">
        <div>
          <label
            id="web-auth-challenge-label"
            for="web-auth-challenge-value"
            class="block text-xs text-gray-400 mb-1.5"
            >Two-factor authentication code</label
          >
          <input
            id="web-auth-challenge-value"
            type="text"
            required
            maxlength="1024"
            inputmode="numeric"
            autocomplete="one-time-code"
            class="w-full px-3 py-2 bg-gray-900 border border-gray-600 rounded-lg text-white focus:outline-none focus:border-proton"
          />
        </div>
        <div class="flex gap-3">
          <button
            id="web-auth-challenge-button"
            type="submit"
            class="px-4 py-2 bg-proton hover:bg-proton-dark text-white text-sm font-medium rounded-lg transition-colors disabled:opacity-50"
          >
            Continue
          </button>
          <button
            id="web-auth-cancel-button"
            type="button"
            class="px-4 py-2 bg-gray-700 hover:bg-gray-600 text-white text-sm rounded-lg"
          >
            Cancel
          </button>
        </div>
      </form>

      <p id="web-auth-message" class="mt-4 text-sm text-gray-400" aria-live="polite"></p>
      <p class="mt-4 text-xs text-gray-500">
        The Proton password and 2FA code are never written to disk or logs. An unfinished login
        expires after five minutes. HTTPS is mandatory outside localhost.
      </p>
    </div>
  </div>

  <!-- Settings Section -->
  <div>
    <h2 class="text-2xl font-semibold text-white mb-6">Backup configuration</h2>

    <!-- CLI Hint -->
    <div
      class="text-sm text-gray-400 bg-gray-800/50 rounded-lg px-4 py-3 border border-gray-700/50 mb-6"
    >
      Choose upload-only or enable two-way beta per mapping. Advanced options remain available through
      <code class="text-gray-300 bg-gray-700 px-1.5 py-0.5 rounded">proton-drive-sync config</code>.
    </div>

    <div class="space-y-6">
      <!-- Sync Concurrency -->
      <div class="bg-gray-800 rounded-xl border border-gray-700 p-6">
        <div class="flex items-center gap-2 mb-4">
          <h3 class="text-lg font-semibold text-white">Sync Concurrency</h3>
          <div class="relative group">
            {{ICON_INFO}}
            <div
              class="absolute left-1/2 -translate-x-1/2 bottom-full mb-2 px-3 py-2 bg-gray-900 border border-gray-600 rounded-lg text-xs text-gray-300 w-96 opacity-0 invisible group-hover:opacity-100 group-hover:visible transition-all z-10"
            >
              Number of parallel uploads. Large folder trees are discovered in bounded batches; a
              value between 1 and 4 is recommended for a NAS.
            </div>
          </div>
        </div>
        <div class="flex items-center gap-4">
          <input
            type="range"
            id="sync-concurrency"
            min="1"
            max="16"
            value="{{SYNC_CONCURRENCY}}"
            class="flex-1 h-2 bg-gray-700 rounded-lg appearance-none cursor-pointer accent-proton"
            oninput="document.getElementById('concurrency-value').textContent = this.value"
            onchange="saveConfig()"
          />
          <span id="concurrency-value" class="w-8 text-center text-white font-mono"
            >{{SYNC_CONCURRENCY}}</span
          >
        </div>
      </div>

      <!-- Sync Directories -->
      <div class="bg-gray-800 rounded-xl border border-gray-700 p-6">
        <div class="flex items-center justify-between mb-4">
          <h3 class="text-lg font-semibold text-white">Sync Directories</h3>
          <button
            hx-get="/api/modal/add-directory"
            hx-target="body"
            hx-swap="beforeend"
            class="flex items-center gap-2 px-4 py-2 bg-proton hover:bg-proton-dark text-white text-sm font-medium rounded-lg transition-colors"
          >
            {{ICON_PLUS}} Add Directory
          </button>
        </div>

        <div id="sync-dirs-list" class="space-y-3">{{SYNC_DIRS_HTML}}</div>

        <div id="no-dirs-message" class="hidden text-center py-2 text-gray-500">
          <p>No sync directories configured</p>
        </div>
      </div>

      <!-- Next Button (onboarding only) -->
      <div class="flex justify-end gap-4 {{HIDE_NEXT_BUTTON}}">
        <button
          type="button"
          hx-post="/api/complete-onboarding-controls"
          hx-swap="none"
          class="flex items-center gap-1 px-4 py-2 bg-proton hover:bg-proton-dark text-white text-sm font-medium rounded-lg transition-colors"
        >
          Open dashboard {{ICON_ARROW_RIGHT}}
        </button>
      </div>
    </div>
  </div>
</div>
